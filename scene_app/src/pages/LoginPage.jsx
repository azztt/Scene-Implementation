import React from "react";
import ReactDom from "react-dom"
import { useState } from "react";
import { Paper, Grid } from "@mui/material";

const LoginPage = () => {
    [formState, setFormState] = useState({
        username: "",
        password: ""
    })
    const handleChange = (event) => {
        if (event.target.name === "uaername") {
            
        }
    }
}